# 🛒 Мини-система сбора и анализа данных (Интернет-магазин)

Проект реализует end-to-end систему сбора и анализа данных о потоке заказов, используя Docker Compose.

## ⚙️ Компоненты Системы

**Генератор данных (Python):** Создает записи о заказах (товар, категория, цена, город) и записывает их в БД каждую секунду.Генератор работает автоматически при запуске контейнеров.
**PostgreSQL:** Хранит все сгенерированные данные о заказах.
**Redash:** Используется для создания дашбордов и визуализации ключевых метрик.
**Jupyter Notebook:** Файл `notebook/analysis.ipynb` для анализа данных с помощью pandas.

## 🚀 Запуск Системы

Для запуска требуется установленный **Docker Desktop**.

1.  Убедитесь, что вы находитесь в корневой папке проекта (`data_pipeline_ecommerce`).
2.  Выполните команду для сборки и запуска всех сервисов:
    ```bash
    docker-compose up --build -d
    ```
    *Система должна запуститься одной командой.*

3.  **Redash** станет доступен по адресу: `http://localhost:5000`.