# 🛒 Мини-система сбора и анализа данных (Интернет-магазин)

**Цель проекта:** Создание простой end-to-end системы сбора, хранения и анализа данных о потоке заказов интернет-магазина, полностью развернутой с помощью Docker Compose.

## ⚙️ Компоненты Системы

Система состоит из четырех сервисов, работающих в одной Docker-сети:

1.  **PostgreSQL (`postgres`):** Основная база данных для хранения транзакционных данных (`ecommerce_db`) и служебных метаданных Redash (`redash_db`).
2.  **Генератор данных (`generator`):** Python-скрипт, который каждую секунду создает реалистичные записи о заказах (товар, категория, цена, город) и записывает их напрямую в БД. **Работает автоматически** при запуске контейнеров.
3.  **Redis (`redis`):** Брокер сообщений и кэш, необходимый для работы Redash.
4.  **Redash (`redash_server`, `redash_worker`):** Инструмент для создания дашбордов и визуализации ключевых метрик. **Обязательное требование:** создание дашборда с 3+ визуализациями.

## 🚀 Запуск Системы

Для запуска требуется установленный **Docker Desktop** или **Docker Engine**.

1.  **Перейдите в корневую папку проекта:**
    ```bash
    cd data_pipeline_ecommerce
    ```

2.  **Очистка (рекомендуется для первого запуска):**
    Удалите старые контейнеры и тома, чтобы обеспечить чистую инициализацию базы данных:
    ```bash
    docker-compose down -v
    ```

3.  **Сборка и запуск:**
    Выполните команду для сборки образов и запуска всех сервисов в фоновом режиме:
    ```bash
    docker-compose up -d --build
    ```

## 🔗 Точки доступа

| Сервис | Адрес | Детали доступа |
| :--- | :--- | :--- |
| **Redash (Дашборды)** | **`http://localhost:5000`** | 
| **PostgreSQL (Прямой доступ)** | Порт `5432` | Host: `localhost`, User/Pass: `user`/`password`, DB: `ecommerce_db` |
